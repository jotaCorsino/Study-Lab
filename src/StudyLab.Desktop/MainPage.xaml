<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="StudyLab.Desktop.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

    <Grid Padding="32" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <CommandBar
            DefaultLabelPosition="Right"
            IsDynamicOverflowEnabled="True">
            <CommandBar.Content>
                <StackPanel Spacing="6">
                    <TextBlock
                        FontSize="28"
                        FontWeight="SemiBold"
                        Text="Catalogo" />
                    <TextBlock
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        Text="{Binding CourseCountText}" />
                </StackPanel>
            </CommandBar.Content>
            <AppBarButton
                x:Name="ImportCourseButton"
                AutomationProperties.Name="Importar curso"
                Click="ImportCourseButton_Click"
                Label="Importar curso">
                <AppBarButton.Icon>
                    <SymbolIcon Symbol="OpenFile" />
                </AppBarButton.Icon>
            </AppBarButton>
        </CommandBar>

        <InfoBar
            Grid.Row="1"
            IsOpen="True"
            Message="{Binding StatusMessage}"
            Severity="Informational" />

        <InfoBar
            Grid.Row="2"
            AutomationProperties.Name="Arquivos ignorados na importacao"
            IsOpen="{Binding HasRejectedFiles}"
            Message="Itens ignorados nao foram adicionados ao catalogo."
            Severity="Warning"
            Title="{Binding RejectedFilesSummary}">
            <ListView
                MaxHeight="180"
                ItemsSource="{Binding RejectedFiles}"
                SelectionMode="None">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="0,8">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <TextBlock
                                FontWeight="SemiBold"
                                Text="{Binding Location}" />
                            <TextBlock
                                Grid.Row="1"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                Text="{Binding ReasonText}" />
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </InfoBar>

        <ListView
            x:Name="CourseListView"
            Grid.Row="3"
            AutomationProperties.Name="Cursos importados"
            IsItemClickEnabled="True"
            ItemsSource="{Binding Courses}"
            ItemClick="CourseListView_ItemClick"
            SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="0,12">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <TextBlock
                            FontSize="18"
                            FontWeight="SemiBold"
                            Text="{Binding Title}" />
                        <TextBlock
                            Grid.Row="1"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            Text="{Binding LessonCountText}" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
