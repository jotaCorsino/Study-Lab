<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="StudyLab.Desktop.CourseDetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

    <Page.Resources>
        <DataTemplate x:Key="CourseTreeItemTemplate">
            <Grid Padding="0,8" ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <FontIcon
                    AutomationProperties.AccessibilityView="Raw"
                    FontSize="16"
                    Glyph="{Binding Content.IconGlyph}"
                    VerticalAlignment="Center" />

                <StackPanel Grid.Column="1" Spacing="2">
                    <TextBlock
                        FontWeight="SemiBold"
                        Text="{Binding Content.Title}" />
                    <TextBlock
                        FontSize="12"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        Text="{Binding Content.KindText}" />
                </StackPanel>
            </Grid>
        </DataTemplate>
    </Page.Resources>

    <Grid Padding="32" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <CommandBar
            DefaultLabelPosition="Right"
            IsDynamicOverflowEnabled="True">
            <CommandBar.Content>
                <StackPanel Spacing="6">
                    <TextBlock
                        FontSize="28"
                        FontWeight="SemiBold"
                        Text="{Binding Title}" />
                    <TextBlock
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        Text="{Binding LessonCountText}" />
                </StackPanel>
            </CommandBar.Content>
            <AppBarButton
                AutomationProperties.Name="Voltar para catalogo"
                Click="BackButton_Click"
                Label="Voltar">
                <AppBarButton.Icon>
                    <SymbolIcon Symbol="Back" />
                </AppBarButton.Icon>
            </AppBarButton>
        </CommandBar>

        <InfoBar
            Grid.Row="1"
            IsOpen="{Binding IsFound}"
            Message="{Binding ImportedAtText}"
            Severity="Informational" />

        <TreeView
            x:Name="CourseTreeView"
            Grid.Row="2"
            AutomationProperties.Name="Estrutura do curso"
            ItemTemplate="{StaticResource CourseTreeItemTemplate}"
            ItemInvoked="CourseTreeView_ItemInvoked"
            SelectionMode="None" />
    </Grid>
</Page>
